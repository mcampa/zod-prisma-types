// Target of the `@zod.import` rich comment in full.prisma. The generated file
// imports this by relative path, so it also pins down the import-path handling.

export const isValidName = (value: string) => value.trim().length > 0;
